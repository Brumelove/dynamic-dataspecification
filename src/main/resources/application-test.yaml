server:
  port: 8082
  servlet:
    context-path: /dynamic-api

logging:
  level:
    org:
      springframework:
        web: INFO
      hibernate: INFO

spring:
  application:
    name: DynamicSpecification
    main:
      allow-bean-definition-overriding: true
